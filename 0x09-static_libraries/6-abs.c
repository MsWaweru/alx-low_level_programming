#include "main.h"

/**
 * _abs - computing absolute values of integers
 * @d: function parameter
 * Return: absolute value of int
 */
int _abs(int d)
{
	if (d >= 0)
	{
		return (d);
	}
	return (-d);
}
